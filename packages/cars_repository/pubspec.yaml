name: cars_repository
description: Cars Repository
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  cloud_firestore: ^3.1.8
  equatable: ^2.0.3
  firebase_core: ^1.12.0
  flutter:
    sdk: flutter

dev_dependencies:
  fake_cloud_firestore: ^1.2.1
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0
